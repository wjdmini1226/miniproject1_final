<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.dao.MemberDao">

	<select id="selectList" resultType="member">
		SELECT * FROM member
		ORDER BY m_idx DESC
	</select>
	
	<select id="selectOne" parameterType="int" resultType="member">
		SELECT * FROM member
		WHERE m_idx = #{m_idx}
	</select>
	
	<select id="selectById" parameterType="String" resultType="member">
		SELECT * FROM member
		WHERE m_id = #{m_id}
	</select>
	
	<select id="checkDuplicate" parameterType="map" resultType="int">
		SELECT COUNT(*) FROM member
		<where>
			<if test="m_id != null">
				m_id = #{m_id}
			</if>
			<if test="m_nickname != null">
				OR m_nickname = #{m_nickname}
			</if>
		</where>
	</select>
	
	<insert id="insert" parameterType="member">
		<selectKey keyProperty="m_idx" resultType="int" order="BEFORE">
			SELECT seq_member.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO member(
				m_idx,
				m_id,
				m_pwd,
				m_nickname,
				m_admin
			)VALUES(
				#{m_idx},
				#{m_id},
				#{m_pwd},
				#{m_nickname},
				#{m_admin}
			)
	</insert>
	
	<update id="update" parameterType="member">
	    UPDATE member
	    	SET 	m_pwd 		= 	#{m_pwd}, 
	    			m_nickname 	= 	#{m_nickname}
	    	WHERE	m_idx 		= 	#{m_idx}
	</update>

	<delete id="delete" parameterType="int">
		DELETE FROM member
		WHERE m_idx = #{m_idx}
	</delete>

</mapper>